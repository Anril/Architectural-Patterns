<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.architecturalpatterns">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".tasks_list.TasksListActivity"
            android:label="@string/title_tasks">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".edit_task.EditTaskActivity"
            android:label="@string/title_edit_task" />

        <activity
            android:name=".add_task.AddTaskActivity"
            android:label="@string/title_add_task" />

    </application>

</manifest>